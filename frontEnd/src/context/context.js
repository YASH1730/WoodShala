import {createContext} from 'react';

export const Auth = createContext(); 

// for slidding from around the web app 
export const OpenBox = createContext(); 

// for dark mode chage
export const Mode = createContext();

// for scakbar / alert

export const Notify = createContext();